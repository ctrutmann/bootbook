<div class="col-sm-3 col-md-2 sidebar">
  <div class="row placeholders-sidebar">
    <div class="col-xs-6 col-sm-3 placeholder-sidebar">
      <img src="<%= @user.profile_image %>" width="200" height="200" class="img-responsive" alt="Boot profile image">
      <h4><%=  @user.name %></h4>
      <span class="text-muted">
        <% if @user.is_graduate %>
          Graduate
        <% else %>
          Student
        <% end %>
      </span>

    </div>
  </div>
  <!-- Social Links -->
  <div class='social-links'>
    <% if @user.linkedin %>
      <a href="<%= @user.linkedin %>" target="_blank" class="btn btn-social-icon btn-linkedin">
        <span class="fa fa-linkedin"></span>
      </a>
    <% end %>

    <% if @user.github %>
      <a href="<%= @user.github %>" target="_blank" class="btn btn-social-icon btn-github">
        <span class="fa fa-github"></span>
      </a>
    <% end %>

    <% if @user.facebook %>
      <a href="<%= @user.facebook %>" target="_blank" class="btn btn-social-icon btn-facebook">
        <span class="fa fa-facebook"></span>
      </a>
    <% end %>

    <% if @user.twitter %>
      <a href="<%= @user.twitter %>" target="_blank" class="btn btn-social-icon btn-twitter">
        <span class="fa fa-twitter"></span>
      </a>
    <% end %>
    <br>

    <% if @user.employ %>
      <a href="<%= @user.employ %>" target="_blank"> Employ Profile</a>
    <% end %>
    <br>

    <% if @user.personal_website %>
      <a href="<%= @user.personal_website %>" target="_blank">Personal Website</a>
    <% end %>
  </div>
  <!-- DBC Info -->
  <div class='dbc-info'>
    <p>Quirk: <%= @user.quirk %></p>
    <p>Love: <%= @user.love %></p>
    <p><% @user.cohorts.reverse.each do |cohort| %>
      <%= cohort.name %><br>
    <% end %>
    <%= @user.cohorts.last.campus %> '<%= @user.cohorts.last.graduation_date.to_s.slice(2,2) %>
    </p>
  </div>
  <!-- Location Info -->
  <div class='location-info'>
    <% if @user.country == 'US' %>
      <p><%= @user.city %>, <%= @user.state %></p>
    <% else %>
      <p><%= @user.city %>, <%= @user.country %></p>
    <% end %>
  </div>
  <!-- Employment Info -->
  <% if @user.employment_status != 'unemployed' %>
    <div class='employment-info'>
      <p>Employer: <%= @user.employer %></p>
      <p>Role: <%= @user.role %></p>
    </div>
  <% end %>
  <!-- Interests -->
  <div class='interests-info'>
    <p>Interests:<br>
    <% @user.interests.each do |interest| %>
      <%= interest.interest %><br>
    <% end %></p>
  </div>
  <% if @user == current_user %>
    <div>
      <a href="/conversations"><button type="button" class="messages btn btn-primary btn-small">Messages</button></a>
    </div>
    <div>
      <a href="/users/<%= @user.id %>/edit"><button type="button" class="btn btn-primary btn-small">Edit</button></a>
    </div>
  <% else %>
    <div>
      <%= form_tag("/conversations", method: "post") do %>
        <%= hidden_field_tag(:conversation_variety_id, "1") %>
        <%= hidden_field_tag(:recipient_id, @user.id) %>
        <%= hidden_field_tag(:sender_id, current_user.id) %>
        <%= submit_tag("Message") %>
      <% end %>
      <%= form_tag("/conversations", method: "post") do %>
        <%= hidden_field_tag(:conversation_variety_id, "2") %>
        <%= hidden_field_tag(:recipient_id, @user.id) %>
        <%= hidden_field_tag(:sender_id, current_user.id) %>
        <%= submit_tag("Mentor") %>
      <% end %>
      <%= form_tag("/conversations", method: "post") do %>
        <%= hidden_field_tag(:conversation_variety_id, "3") %>
        <%= hidden_field_tag(:recipient_id, @user.id) %>
        <%= hidden_field_tag(:sender_id, current_user.id) %>
        <%= submit_tag("Collaborate") %>
      <% end %>
      <!-- <a href="/conversations"><button type="button" class="default-message btn btn-primary btn-small">Message</button></a> -->
      <!-- <a href="/conversations"><button type="button" class="mentor-message btn btn-primary btn-small"><i class="fa fa-graduation-cap" aria-hidden="true"></i> Mentor</button></a>
      <a href="/conversations"><button type="button" class="collaborate-message btn btn-primary btn-small"><i class="fa fa-puzzle-piece" aria-hidden="true"></i> Collab</button></a> -->
    </div>
  <% end %>
</div>

<div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
<% if flash[:danger] %>
  <div class="alert alert-danger">
    <% flash[:danger].each do |error|%>
      <%= error %><br>
    <% end %>
  </div>
  <% elsif flash[:success] %>
  <div class="alert alert-success">
    <%= flash[:success] %>
  </div>
<% end %>

<div>
  <div class='profile-text-box'>
    <h1>Bio</h1>
    <p><%= @user.bio %></p>
  </div>

  <div class='profile-text-box'>
    <h1>Tweets of Advice to Students</h1>
    <p><%= @user.advice_to_students %></p>
  </div>

  <div class='profile-text-box'>
    <h1>Tweets of Advice to Graduates</h1>
    <p><%= @user.advice_to_graduates %></p>
  </div>
</div>
